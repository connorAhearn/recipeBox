import React from 'react';
import PropTypes from 'prop-types';
import './RecipeSubmit.css';

const RecipeSubmit = () => (
  <div className="RecipeSubmit" data-testid="RecipeSubmit">
    Recipe-submit Component
  </div>
);

RecipeSubmit.propTypes = {};

RecipeSubmit.defaultProps = {};

export default RecipeSubmit;
